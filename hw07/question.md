# 作业7：用三种适应算法处理同一作业序列

## 题目描述

现有一作业序列，A(15k)，B(16k)，C(15k)，依次要求进入系统运行。假设此时系统内存分配情况如图所示。

1. 请用首次适应算法、最佳适应算法和最差适应算法来处理该作业序列，看哪种算法是合适的。
2. 若将任务顺序改为B、A、C，再进行1中的分配过程。

## 内存使用情况图表

| 内存地址范围 | 分区状态 / 类型 | 分区大小 |
| --- | --- | --- |
| 0 — 40KB | 操作系统 | 40KB |
| 40KB — 56KB | **空闲区1** | **16KB** |
| 56KB — 70KB | 已使用 | 14KB |
| 70KB — 84KB | **空闲区2** | **14KB** |
| 84KB — 100KB | 已使用 | 16KB |
| 100KB — 105KB | **空闲区3** | **5KB** |
| 105KB — 150KB | 已使用 | 45KB |
| 150KB — 180KB | **空闲区4** | **30KB** |
| 180KB — 256KB | 已使用 | 76KB |

## 分析

- 为了简单明了，我们把三种算法的空闲区用链表形式组织起来，如图 (a)、(b)、(c) 所示。
- 在图中，由三种算法的空闲区链表组织形式，结合动态分区分配，可以分析得出这样的结论：首次适应算法和最佳适应算法都只能分配作业A和作业B，不能再分配作业C，即不能对这三个作业进行完全吞吐。只有最差适应算法，可将作业A、作业B、作业C这三个作业序列的三个作业全部分配，所以最差适应算法对该作业序列而言是合适的。
- 若作业进入内存的顺序依次为：B -> A -> C，则首次适应算法和最佳适应算法都能将该作业序列完全吞吐，而最差适应算法不能将该作业系列完全吞吐。

> **节点结构说明**：
> 链表节点上半部分为**当前空闲区大小**，下半部分为**下一个空闲区的首地址**（`^` 表示空指针）。

### (a) 首次适应算法空闲区链表组织（按地址递增排序）

```text
             ┌───────┐    ┌───────┐    ┌───────┐    ┌───────┐
 [ 40KB ] ──>│ 16KB  │ ┌─>│ 14KB  │ ┌─>│  5KB  │ ┌─>│ 30KB  │
 链表头指针  ├───────┤ │  ├───────┤ │  ├───────┤ │  ├───────┤
             │ 70KB  │-┘  │ 100KB │-┘  │ 150KB │-┘  │   ^   │
             └───────┘    └───────┘    └───────┘    └───────┘
```

### (b) 最佳适应算法空闲区链表组织（按容量递增排序）

```text
              ┌───────┐    ┌───────┐    ┌───────┐    ┌───────┐
 [ 100KB ] ──>│  5KB  │ ┌─>│ 14KB  │ ┌─>│ 16KB  │ ┌─>│ 30KB  │
 链表头指针   ├───────┤ │  ├───────┤ │  ├───────┤ │  ├───────┤
              │ 70KB  │-┘  │ 40KB  │-┘  │ 150KB │-┘  │   ^   │
              └───────┘    └───────┘    └───────┘    └───────┘
```

### (c) 最差适应算法空闲区链表组织（按容量递减排序）
```text
              ┌───────┐    ┌───────┐    ┌───────┐    ┌───────┐
 [ 150KB ] ──>│ 30KB  │ ┌─>│ 16KB  │ ┌─>│ 14KB  │ ┌─>│  5KB  │
 链表头指针   ├───────┤ │  ├───────┤ │  ├───────┤ │  ├───────┤
              │ 40KB  │-┘  │ 70KB  │-┘  │ 100KB │-┘  │   ^   │
              └───────┘    └───────┘    └───────┘    └───────┘
```
